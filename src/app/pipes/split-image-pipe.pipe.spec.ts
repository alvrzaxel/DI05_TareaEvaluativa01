import { SplitImagePipePipe } from './split-image-pipe.pipe';

describe('SplitImagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitImagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
